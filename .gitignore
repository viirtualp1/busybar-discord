node_modules/
dist/
.env
*.png
!docs/*.png
.DS_Store

# The OAuth token this app trades for, and the avatars it caches. Anchored:
# an unanchored `avatars/` also matches src/avatars, which is the code.
discord-token.json
/avatars/
/preview/
